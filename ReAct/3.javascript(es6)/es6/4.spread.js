const originalSectors = ['IT', '바이오']
const newSectors = [...originalSectors, "로봇", "AI"]

const [first, sencond] = newSectors
console.log(`주력 섹터: ${first} ${sencond}`)
console.log(`전체 섹터: ${newSectors}`)